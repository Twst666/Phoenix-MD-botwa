

const toBool = (x) => x === 'true';

module.exports = {
    SESSION_ID: process.env.SESSION_ID || 'Phoenix~UnMG3VSF',
    HANDLERS: process.env.HANDLER || '.',
    BOT_NAME: process.env.BOT_NAME || 'Phoenix-MD',
    SUDO: process.env.SUDO || '917025673121',
};
